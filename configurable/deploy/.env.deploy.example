# 复制为 .env.deploy 后填写（.env.deploy 已 gitignore）
# 在仓库根执行: bash configurable/deploy/push-deploy.sh

# 必填：服务器 IP 或域名
DEPLOY_HOST=124.222.39.165

# 可选
DEPLOY_USER=ubuntu
# 请用绝对路径，避免 ~ 在本地被错误展开
DEPLOY_PATH=/home/ubuntu/music

# 设为 1 时 rsync 会 --delete（删除远端有而本地没有的目录内文件，慎用）
# DEPLOY_RSYNC_DELETE=0
